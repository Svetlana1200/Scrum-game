export enum Role {
    LISTENER = 'слушатель',
    AUTHOR = 'автор',
    MODERATOR = 'модератор'
}

export enum Metric {
    ARPU = 'ARPU',
    RR = 'Retation rate'
}

export enum Advertising {
    BIG = 'большая',
    MEDIUM = 'средняя',
    LITTLE = 'маленькая'
}